[project]
name = "picture-to-3d"
version = "0.2.0"
description = "Image to 3D model using TripoSR"
requires-python = ">=3.12"

[project.scripts]
serve = "uvicorn api.main:app --host 0.0.0.0 --port 8080"

[dependency-groups]
dev = []
