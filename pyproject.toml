[project]
name = "picture-to-3d"
version = "0.3.0"
description = "Image to 3D model using TripoSR"
requires-python = ">=3.12"

[project.scripts]
serve = "api.main:serve"

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
